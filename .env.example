# MCP Server Configuration
# Base URL for the MCP server (required for health checks and metrics)
MCP_BASE_URL=https://your-mcp-server.com
# Or use NEXT_PUBLIC_MCP_BASE_URL if you need client-side access
# NEXT_PUBLIC_MCP_BASE_URL=https://your-mcp-server.com

# Admin Token (required for admin endpoints)
MCP_ADMIN_TOKEN=your-admin-token-here
# Or use NEXT_PUBLIC_MCP_ADMIN_TOKEN if you need client-side access
# NEXT_PUBLIC_MCP_ADMIN_TOKEN=your-admin-token-here

# Application URL (for internal API calls)
NEXT_PUBLIC_APP_URL=http://localhost:3000
